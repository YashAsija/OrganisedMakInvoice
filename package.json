{
  "name": "makinvoices-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build && node -e \"fs.cpSync('frontend/.next', '.next', { recursive: true })\"",
    "start": "npm --prefix frontend run start"
  }
}
